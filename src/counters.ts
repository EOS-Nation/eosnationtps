export var actions = 0;
export var transactions = 0;
export var errors = 0;

export function setActions(num: number) { actions = num }
export function setTransactions(num: number) { transactions = num }
export function setErrors(num: number) { errors = num }
